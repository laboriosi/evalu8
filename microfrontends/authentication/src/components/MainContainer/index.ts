import { Column } from '@laboriosi/evalu8-ui-library'
import styled from 'styled-components'

const MainContainer = styled(Column)`
  width: 350px;
  margin-top: 140px;
  margin-right: auto;
  margin-left: auto;
`

export default MainContainer
